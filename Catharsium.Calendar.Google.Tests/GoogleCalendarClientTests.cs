using Catharsium.Calendar.Google.Core.Entities.Interfaces;
using Catharsium.Util.Testing;
using Google.Apis.Calendar.v3;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using NSubstitute;

namespace Catharsium.Calendar.Google.Tests
{
    [TestClass]
    public class GoogleCalendarClientTests : TestFixture<CalendarClient>
    {
        //[TestMethod]
        //public void x()
        //{
        //    var service = Substitute.For<CalendarService>();
        //    var s = Substitute.For<IGoogleCalendarServiceFactory>();
        //    this.SetDependency< IGoogleCalendarServiceFactory>(new T());
        //  //  this.GetDependency<IGoogleCalendarServiceFactory>().CreateService().Returns(service);
        //    var actual = this.Target.GetCalendars();
        //}
    }
}
